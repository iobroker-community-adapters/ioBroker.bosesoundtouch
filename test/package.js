const path = require('path');
const { tests } = require('@iobroker/testing');

// Validate the package files
console.log( 'WARNING: Package tests disabled');
// disable for not tests.packageFiles(path.join(__dirname, '..'));
